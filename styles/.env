    background-image: linear-gradient(135deg, rgb(135, 206, 235), rgb(255, 223, 186)); /* Sky blue to warm sunlight yellow */
    background-image: linear-gradient(135deg, rgb(34, 193, 195), rgb(253, 187, 45)); /* Blue-green to soft orange gradient */
    background-image: linear-gradient(135deg, rgb(255, 140, 0), rgb(255, 255, 255)); /* Enhanced orange gradient */
    background-image: linear-gradient(rgb(247, 151, 92), rgb(255, 255, 255));


/*

/* General Styles for Profile Section */
.profile-info {
    width: 100%;
    max-width: 600px; /* Limit the width for better readability */
    background-color: rgba(255, 255, 255, 0.95); /* Slightly transparent white background */
    border-radius: 10px;
    padding: 20px;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1); /* Soft shadow for depth */
    margin: 20px auto; /* Center the profile info */
    text-align: left; /* Align text to the left for better readability */
}

/* Heading Styles */
.profile-info h2 {
    color: #333; /* Dark color for headings */
    margin-bottom: 15px;
}

.profile-info h1 {
    font-size: 28px; /* Larger font size for the name */
    color: #FF8C00; /* Bright orange color to match your theme */
    margin: 10px 0; /* Margin for spacing */
}

/* Paragraph Styles */
.profile-info p {
    font-size: 16px; /* Standard font size for text */
    color: #555; /* Slightly muted color for the text */
    margin: 5px 0; /* Margin for spacing between lines */
}

/* Button Styles */
.profile-info button {
    padding: 10px 15px;
    background-color: #FF8C00; /* Bright orange button */
    color: white;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    font-weight: bold;
    transition: background-color 0.3s ease;
    margin-top: 20px; /* Margin for spacing */
}

.profile-info button:hover {
    background-color: #FF6347; /* Lighter coral color on hover */
}

/* Edit Profile Section */
.edit-profile {
    display: none; /* Hidden by default */
    margin-top: 20px; /* Space above the edit form */
    padding: 20px;
    background-color: rgba(240, 240, 240, 0.95); /* Light grey background for the edit section */
    border-radius: 10px; /* Rounded corners */
}

.edit-profile label {
    display: block; /* Block display for labels */
    margin-bottom: 5px; /* Space below labels */
    color: #333; /* Darker label color */
}

.edit-profile input[type="text"],
.edit-profile input[type="email"],
.edit-profile input[type="password"] {
    width: 100%; /* Full width inputs */
    padding: 10px;
    margin-bottom: 15px; /* Space below inputs */
    border: 1px solid #ccc; /* Light border */
    border-radius: 5px; /* Rounded corners */
    box-sizing: border-box; /* Include padding in width */
}

/* Submit Button for Edit Form */
.edit-profile input[type="submit"] {
    background-color: #FF8C00; /* Bright orange */
    color: white;
    border: none;
    border-radius: 5px;
    padding: 10px;
    cursor: pointer;
    font-weight: bold;
    transition: background-color 0.3s ease;
}

.edit-profile input[type="submit"]:hover {
    background-color: #FF6347; /* Lighter coral on hover */
}

/* Responsive Design */
@media (max-width: 768px) {
    .profile-info {
        width: 90%; /* Responsive width on smaller screens */
    }
}

*/